# 本地资源配置清单 — <service-name>

> **⚠️ 重要提示**
> 本文件是模板文件，供复制使用。
> 复制为 `LOCAL_CONFIG.md` 后**不应提交到 Git 仓库**。
> 详细规范请参考：rule 21 — 本地配置协议

---

## 更新记录

| 日期 | 更新人 | 说明 |
|------|--------|------|
| YYYY-MM-DD | - | 初始创建 |

---

## 端口占用

| 端口 | 服务名称 | 用途 | 备注 |
|------|----------|------|------|
| - | <service-name> | - | - |

---

## 数据库配置

| 数据库类型 | 主机 | 端口 | 数据库名 | 密码 |
|------------|------|------|----------|------|
| PostgreSQL | localhost | 5432 | <db_name> | 见密码管理器 |
| Redis | localhost | 6379 | - | 见密码管理器 |

---

## API 密钥与凭据

| 服务 | Key/Secret | 环境 | 有效期 |
|------|-----------|------|--------|
| - | 见密码管理器 | sandbox | - |

---

## 快速查询命令

```bash
# 查看端口占用
lsof -i :<PORT>

# 容器状态
docker ps -a | grep <service-name>

# 容器日志
docker logs <container-name>

# 运行自检
./doctor.sh

# 数据库连接
psql -h localhost -p 5432 -U <user> -d <db_name>
```
