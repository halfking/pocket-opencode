# Copy this outside Git's ignored .env.* profiles as .env.harmony-dev, or set
# VITE_API_BASE at build time. ArkWeb rawfile builds must target a device-
# reachable LAN HTTPS/WSS endpoint. Never use localhost or Android emulator 10.0.2.2.
VITE_API_BASE=https://pocket.example.invalid
VITE_APP_ENV=dev
