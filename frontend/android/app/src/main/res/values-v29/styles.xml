<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Android 10+ (API 29) edge-to-edge 主题补丁：
      - windowLayoutInDisplayCutoutMode=shortEdges：挖孔屏允许内容延伸到刘海下方
      - statusBarColor=@transparent：配合 MainActivity 的
        WindowCompat.setDecorFitsSystemWindows(false) 让 status bar 真正透明
      - navigationBarColor 同样透明，便于底部 safe-area-bottom 准确取 env()
    -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
