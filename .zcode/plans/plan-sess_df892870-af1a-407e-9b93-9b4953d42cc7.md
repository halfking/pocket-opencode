1. Establish an evidence-backed baseline:
   - Inspect current branch, working tree, recent commits, and the complete latest diff without overwriting existing user files.
   - Run `go test ./...`, targeted race tests, `go vet ./...`, and backend build checks; record exact failures.
   - Inspect Android/iOS source, Capacitor/build configuration, deployment manifests/scripts, and the latest documentation/report files for drift.

2. Fix the highest-impact defects found in the current code:
   - Put every state-changing or sensitive API behind the same JWT middleware and make tenant/user identity come from authenticated context rather than caller-controlled headers/body/query values.
   - Close IDOR paths in notes, snippets, tasks, and related stores by carrying the authenticated user/workspace boundary into reads, updates, deletes, and links; preserve legacy/default workspace behavior only where explicitly safe.
   - Restrict STT/file inputs, enforce request/body size limits, and prevent arbitrary local path reads; normalize errors and avoid returning upstream/internal secrets.
   - Harden WebSocket origin/auth behavior, remove unauthenticated identity fallbacks, and ensure long-lived SSE/WebSocket connections are not killed by the HTTP server write timeout.
   - Audit and fix ACP stdio transport lifecycle/concurrency issues (serialized writes, cancellation/pending-call cleanup, safe close/error handling, and subscription cleanup) where tests or code inspection confirm them.
   - Add regression tests for each corrected authorization, tenant isolation, input-size/path, WebSocket, and transport behavior.

3. Validate all changes:
   - Re-run full tests, race tests, vet, backend build, and available Android/iOS builds/tests.
   - Re-check deployment and docs against actual environment names, ports, commands, endpoints, and generated artifact paths; update only stale/inaccurate material relevant to the implementation.
   - Review `git diff`, secret exposure, generated files, and the final test/build output.

4. Deliver and report:
   - Create a detailed audit report with findings, severity, affected files, fixes, residual risks, and exact verification commands/results.
   - Commit the scoped fixes and report, then push the commit to `main` as requested.
   - Return the commit ID, push result, test/build status, and any platform-toolchain limitations plainly.