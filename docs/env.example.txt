# POCKET_HTTP_PORT=18088
POCKET_HTTP_PORT=18088

# === PostgreSQL（可选，Phase 0/1 迁的本地任务缓存）===
# 留空 = remote-only 模式（仅依赖 ACC/OpenCode/llm-gateway 远程）
# 示例：POCKET_POSTGRES_DSN=postgres://user:pass@host:5432/dbname?sslmode=disable
# POCKET_POSTGRES_DSN=

# === AI/STT 配置 ===

# Groq STT（可选，Whisper Large v3 Turbo 兜底）
POCKET_GROQ_API_KEY=

# 嵌入（OpenAI 兼容）
# 留空 = /api/embed 返回 503
POCKET_EMBED_BASE_URL=https://api.openai.com/v1
POCKET_EMBED_API_KEY=
POCKET_EMBED_MODEL=text-embedding-3-small

# LLM（OpenAI 兼容，Groq 便宜快）
# 留空 = /api/llm/chat 返回 503
POCKET_LLM_BASE_URL=https://api.groq.com/openai/v1
POCKET_LLM_API_KEY=
POCKET_LLM_MODEL=llama-3.3-70b-versatile

# === llm-gateway-go 企业网关（可选，覆盖上面 LLM/Embed 配置）===
# 配了 POCKET_LLM_GATEWAY_URL + POCKET_LLM_GATEWAY_API_KEY 后，pocketd 走企业网关
# 同时使用 llm-gateway 自身的 provider 池（OpenAI/Anthropic/国产 LLM 等）
# POCKET_LLM_GATEWAY_URL=
# POCKET_LLM_GATEWAY_API_KEY=

# === kxmemory AI 编排（可选）===
# 提供：笔记分类 / SSOT 检测 / 邮件分类 / 每日邮件总结
# 留空 = handleNotes/handleEmails 跳过 AI 分类（仅入库）
POCKET_KXMEMORY_BASE_URL=

# === ACC MCP 任务集成（可选）===
# 提供：acc_get_tasks → source=acc 任务源 + 5 分钟后台同步
# 留空 = 任务三源聚合跳过 acc 源
POCKET_MCP_BASE_URL=
POCKET_MCP_API_KEY=

# === 邮箱模块（Phase 2 完整功能）===
# 用 AES-GCM 加密 IMAP 凭证后存本地库（即使不上云也安全）
POCKET_EMAIL_MASTER_KEY=

# === 飞书事件回调（已有功能）===
POCKET_FEISHU_APP_ID=
POCKET_FEISHU_APP_SECRET=
POCKET_FEISHU_VERIFY_TOKEN=
POCKET_FEISHU_VERIFY_SECRET=
POCKET_FEISHU_ENCRYPT_KEY=

# === OpenCode 实例发现（已有功能）===
POCKET_INSTANCE_DISCOVERY_BASE_URL=
POCKET_INSTANCE_DISCOVERY_AUTH_TOKEN=
POCKET_OPENCODE_INSTANCES=[{"id":"kaixuan1","displayName":"开发实例1","apiBaseURL":"http://10.0.0.71:4096","environment":"dev"}]

# === 应用配置 ===
POCKET_OPENCODE_TIMEOUT_MS=5000
POCKET_ANDROID_APP_ID=com.kaixuan.opencode.pocket
POCKET_ANDROID_USE_CAPACITOR=true

# === 认证（Phase 0 桩接，Phase 1 切真实 JWT）===
# 默认 "pocket-dev-insecure-secret"，生产必须改
POCKET_JWT_SECRET=pocket-dev-insecure-secret
