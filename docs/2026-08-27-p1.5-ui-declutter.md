# P1.5 界面减负——设计决策与实现记录

> **日期**: 2026-08-27（深夜）~ 2026-08-28
> **状态**: implemented（真机验证状态见 §6 与 EVIDENCE-LEDGER）
> **上位规范**: `docs/2026-08-27-mobile-ux-design-v2.md`（§2 设计原则，尤其 §2.2 信号即界面 / §2.3 两步介入）
> **来源**: 用户 2026-08-27 22:53 指示——①多个标题栏部分重叠；②不必要的信息应隐藏、点击打开；③导航加"..."入口；④动态图标表示工作状态（点击即停止，再点又开始）；⑤停止/继续等按钮图标化节约空间
> **范围**: 纯前端（`frontend/`），不动后端契约（P1 冻结的事件 schema 与 Composer props 契约均保持）

---

## 1. 问题盘点（真机证据：P1 验证 shots vivo-04/05）

| # | 问题 | 根因 |
|---|---|---|
| P-1 | 会话工作台顶部 4 层条堆叠：系统状态栏 + 壳层顶栏（"← 会话"）+ 视图 top-bar（标题/实例副标题/info）+ SessionStatusBar 独立行 | 路由 meta `hideAppHeader: true`（router-mobile.ts:232）从未被 AppLayout 消费（只认 `showTopBar`；`hideAppHeader` 仅 ScrollChromePortal 读取）→ 壳层顶栏与全局状态条多渲染 |
| P-2 | Material Symbols 图标全部渲染为 ligature 原文（真机 "rrow_back" 文字） | 工程无任何 @font-face / Google Fonts link / npm 包加载该字体；国内设备也无法依赖 fonts.googleapis.com |
| P-3 | 模板 chips（继续/停下/总结当前进展/跑测试/忽略错误继续）常驻占整行 | P1 设计原文（§4.4-2 输入框上方一行） |
| P-4 | 实例副标题（"本地 OpenCode 实例"）常驻头部 | P1 头部副标题；非实时信息常驻违背分层披露 |
| P-5 | 头部按钮（返回/info）热区 32px < 44px 纪律 | P1 存量偏差 |

## 2. 改动总览（文件 → 内容）

| 文件 | 改动 |
|---|---|
| `app/AppLayout.vue` | **修 P-1**：`showTopBar` 追加 `hideAppHeader !== true` 条件（GlobalStatusBar 随之隐藏，同一 computed） |
| `styles/material-symbols.css`（新） | **修 P-2**：@font-face（自托管子集 woff2，内联进 CSS）+ 全局 `.material-symbols-outlined` 类（liga 连字）。一次修复工程 15 个文件的既有图标渲染 |
| `assets/fonts/material-symbols-outlined.woff2`（新，4KB） | 3.8MB variable font → 固定轴值（wght=400 FILL=0 GRAD=0 opsz=24）→ 全量 ligature 子集 → GSUB 过滤目标 ligature → 二次子集。**31 个图标 4KB** |
| `scripts/subset-material-symbols.sh`（新） | 字体再生脚本（扫描源码图标名 + EXTRA_ICONS，venv 自动装 fontTools） |
| `main.ts` | import material-symbols.css |
| `features/sessions/useSessionEvents.ts` | 新增纯函数：`resolveSessionStatusMode`（信号三态）、`sessionStatusLabel`（信号文案）、`formatStatusElapsed`（时长分级格式化）——图标与头部副标题共用，node --test 可测 |
| `features/sessions/SessionStatusBar.vue` | **重写**：一行式状态条 → 44px 圆形动态状态图标按钮（详见 §3） |
| `features/sessions/SessionConversationView.vue` | 头部收敛：`[退出] [状态图标] 标题 + 信号副标题(状态·时长) [⋮]`；删除独立状态条行；实例副标题移除（收进抽屉）；info→more_vert；back-btn 热区 32→44px（修 P-5）；新增 `continueSession()`（走 sendPrompt 同路径） |
| `features/sessions/SessionComposer.vue` | **修 P-3**：chips 常驻行 → 输入行首 `bolt` 快速指令按钮 + BottomSheet 面板（行内完整文案 + 图标，48px 热区）；固定目标模式下目标 chip 不再渲染（见 DD-3）；targets 可切换模式保留 chip 不变 |
| `features/sessions/useSessionDrafts.ts` | `QuickCommand` 增加 `icon?` 字段（契约 §4 文案与顺序不变，纯增量） |
| `features/sessions/SessionDetailDrawer.vue` | **修 P-4**：新增"💻 实例"区块（显示名 + instance_id 截断），props 增 `instanceName?` / `instanceId?` |
| `__tests__/useSessionEvents.test.mjs` | +3 test（信号三态/文案兜底/时长格式化与负数钳制） |

**不动**：`components/BottomNav.vue`（有并行流未提交改动；且底导"⋮ 更多"收纳面板已由 3e4a9a3 落地——任务③的底导部分已存在，本次仅做会话级 ⋮ 收纳，见 DD-4）、`router-mobile.ts`（并行流 WIP）、RoundTimeline（轮头是内容结构非标题栏，P1.5 不扩范围）、后端全部。

## 3. 动态状态图标（核心交付）

```
头部：[←] [◉] 标题          [⋮]
           └ 状态·时长（副标题）

◉ 三态（信号即界面 §2.2——信号即入口）：
  🔴 notifications_active + 待办角标 + 呼吸动画 → 单击 = 查看审批（原 [查看]）
  🟢 progress_activity + 持续旋转             → 单击 = 停止（interruptSession）
  ⚪ play_arrow 弱化                          → 单击 = 继续（sendPrompt('继续')）
```

- 数据源不变：P1 的 `useSessionEvents`（session.activity 五态 phase）+ 审批 store；断线降级 `deriveFallbackPhase` 原样。
- 状态派生抽为纯函数（`resolveSessionStatusMode`/`sessionStatusLabel`），图标组件与头部副标题共用同一份逻辑，防两处漂移。
- 动画遵循全局 `prefers-reduced-motion` 裁剪（styles.css 既有规则）。
- aria-label 携带完整信号（"改文件中 · 40s · 点击停止"），role="status"。

## 4. 设计决策登记（DD）

| # | 决策 | 理由与取舍 |
|---|---|---|
| **DD-1** | **运行态单击停止，无二次确认**；Toast 提示"已停止，点击状态图标可继续"。与"停下二次确认"纪律（设计 v2 §4.4-2）的偏差**显式登记** | ① 停止可经同一图标一次点击恢复（toggle），误触成本 = 一次点击，非不可逆破坏；② 两步介入原则（§2.3）要求高频动作零摩擦，真机 P1 验证中停止是最高频介入；③ 面板内文字"停下"**保留** window.confirm 二次确认不变（用户主动输入场景防误触）；④ 兜底：误停止的最坏后果是重跑一轮，由 Toast 引导恢复 |
| **DD-2** | 状态条收敛为**头部内嵌图标**（而非保留独立行图标化） | 用户指认的问题是"标题栏重叠"——只图标化不合并仍留两层条。图标+副标题合并后：头部一层同时承担导航（返回）、信号（图标+状态·时长）、收纳（⋮）三职责，每层职责唯一 |
| **DD-3** | 固定目标模式下 Composer 目标 chip 不再渲染 | 会话标识已由头部标题承担（P1.5 后信息更全：标题+信号）；chip 显示的截断标题与头部完全重复。**targets 可切换模式保留 chip**（切换职责仍在，契约 §4 就绪待 P2 接线）。释放的横向空间给 textarea |
| **DD-4** | 底导"..."不加新按钮 | 盘点发现 `BottomNav.vue` 已有"⋮ 更多"收纳面板（3e4a9a3 落地：PKM/密码箱/任务/会话/实例/成本/设置），任务③的底导语义已满足；且该文件属并行流 WIP（勿动纪律）。本迭代的"..."落在会话级：⋮ 抽屉（实例信息+统计+轮摘要+导出） |
| **DD-5** | 图标字体**自托管子集**（4KB）而非 CDN/npm 全量（3.8MB）或逐文件改 SVG | ① 国内设备 fonts.googleapis.com 不可达，P-2 必须自托管；② 全量 3.8MB 不可接受（主 chunk 才 594KB）；③ 改 15 个文件为 SVG 侵入过大且与并行流冲突面大；④ 子集保留 liga 连字机制，模板写法（图标名字面量）零改动，并行流 ai-chat 的图标也被一并修复。代价：新增图标需重跑 `scripts/subset-material-symbols.sh`（脚本自动扫描源码，忘跑的症状是图标显示为文字，易发现易修） |
| **DD-6** | 字体轴值固定 wght=400 FILL=0（outlined 常规），不保留可变轴 | 移动端无需运行时变轴；固定后 gvar 数据（体积大头）被剔除，这是 376KB→4KB 的关键一步 |

## 5. 验证

- 单测：`node --test src/features/sessions/__tests__/` 38/38 通过（35 存量 + 3 新增）
- 类型：`npm run typecheck`（vue-tsc）零错误
- 构建：`VITE_API_BASE=http://192.168.31.37:8088 npm run build:fast` → 主 chunk grep API 地址 ✓ → `npx cap sync android` → cap assets CSS 含 `data:font/woff2` ✓
- 真机：见 §6（vivo X Fold5，uninstall+install，CDP 驱动）

## 6. 真机验证记录（vivo X Fold5 / V2436A，2026-08-27 23:14~23:25）

构建链：`VITE_API_BASE=http://192.168.31.37:8088 npm run build:fast`（主 chunk grep API 地址 ✓）→ `npx cap sync android`（assets CSS 含 data:font/woff2 ✓）→ gradle assembleDebug → APK unzip 复验（字体 CSS + API 地址均 ✓）→ adb install（lastUpdateTime=23:14:43 确认更新生效）。后端现场：pocketd@8088（P1 容器）+ opencode serve@4096 + 注册守护（local-opencode 实例）。

| # | 验证点 | 结果 | 证据 |
|---|---|---|---|
| V-1 | 壳层"← 会话"双头消除 | ✅ 工作台仅一层应用头部 | shots/vivo-p15-02-workbench.png |
| V-2 | 头部结构 = [←][状态图标] 标题+信号副标题 [⋮] | ✅ | 同上 |
| V-3 | 图标字体渲染（原 rrow_back 文字） | ✅ 全部图标渲染为字形 | vivo-p15-01/02/03 |
| V-4 | 运行态：旋转动画 + aria"思考中 · Ns · 点击停止" | ✅（computed animationName=status-spin 实测激活） | vivo-p15-05-running.png |
| V-5 | 单击停止：无确认直接执行 + Toast 引导 | ✅ interrupt 204 + Toast"已停止，点击状态图标可继续" | CDP 会话记录 |
| V-6 | 空闲态：play_arrow + aria"空闲 · 点击继续" | ✅ 轮次完成后图标转 idle（mode-idle 实测） | CDP 会话记录 |
| V-7 | 快速指令面板：bolt 入口 + 5 项（图标+完整文案） | ✅ 常驻 chips 行已消除 | vivo-p15-03-quick-panel.png |
| V-8 | ⋮ 抽屉：实例区块（Local OpenCode/local-opencode）+ 统计 + 轮摘要 + 导出 | ✅ | vivo-p15-04-drawer.png |
| V-9 | 单测/类型/构建 | ✅ 38/38 · vue-tsc 0 错 · 构建链三级 grep | CI 复验待 push 后 |

### 6.1 验证中发现的存量缺陷（登记 P2 缺陷池，非 P1.5 引入）

- **DEFECT-P2-SSE-1（高）**：SSE 消息流不向 App 投递 assistant 输出且无流关闭信号。真机对照证据：opencode 侧 23:21:57 轮次已完成（assistant 已回复"链路正常-2"，直查 /session/:id/message 可见），App 侧同轮 100s+ 后仍 isStreaming=true、图标久挂"思考中"、assistant 气泡从未渲染（DOM bubbles=1）。后果：停止/完成信号不可达 → 图标状态机被上游数据钉死在运行态（状态机本身按输入正确运转，见 V-4/V-6）。P1.5 未触碰流/store 层（全部 diff 在展示层）。
- **DEFECT-P2-DUP-1（中，存疑）**：同文 prompt 重复投递一次（23:17:09 与 23:17:45 两条相同 user 消息入 opencode 历史，间隔 36s；两次发送均非用户操作窗口）。需 pocketd/opencode 服务端日志定位（重复 POST 方在 App 或 pocketd 重试逻辑）。
- 以上两项归属 P1 事件/流层（dc16666/e296193 交付），P1.5 仅负责发现与登记；修复排 P2（与 P2"订阅收窄 + since 追赶"同批处理最合适）。

## 7. P1.5+ 追加轮（同日深夜，用户两条反馈）

### 7.1 反馈 A：会话详情页标题距顶过远（全 UI 排查）

**量化**（vivo X Fold5 CDP 实测）：详情页头部距顶 **90px** = 39（body safe-area）+ 39（.session-view 自加 safe-area）+ 12（AppLayout .content padding）；正确值 39px（贴状态栏下沿）。/ai 顶栏内部另有 39px 双重 padding（标题文字被压到 ~78px）。

**根因**：safe-area-inset-top 被**三层各自叠加**（styles.css body + AppLayout top-bar + session-view）。全工程审计 7 处 `safe-area-inset-top`：

| 位置 | 判定 | 处置 |
|---|---|---|
| styles.css body | 全局基线 | **保留**（唯一来源） |
| AppLayout .top-bar | 流内元素，双重 | **删** |
| SessionConversationView .session-view | 流内元素，双重 | **删** |
| SettingsLLMGateway chrome-shell | fixed 定位（body padding 不生效），自管正确 | 不动 |
| AIChatView / Gateway 两页 | **并行流 WIP 文件** | 登记交接（若非 fixed 同样双倍） |
| responsive.css --safe-area-top 变量 | 无消费者 | 不动 |

**DD-8 安全区唯一来源**：`styles.css` 的 body `padding-top: env(safe-area-inset-top)` 是全 App 唯一顶部安全区来源；流内元素（壳层顶栏/全屏视图头部）一律不得再叠加（fixed 定位元素除外——body padding 对其无效，需自管，如 SettingsLLMGateway）。

修复后实测：详情页头部距顶 **39px**（回收 51px）；/ai 标题文字 78→51px。附带：hideAppHeader 全屏页的 .content padding 归零（`fullscreen` class）。

### 7.2 反馈 B：会话活跃/归档两大类（与 Acc 任务关联）

**落地**（SessionListView）：
- 分段切换「活跃(N)｜归档(M)」，默认活跃；归档区显示"已归档"徽标 + 左滑「恢复/删除」，活跃区左滑「归档/删除」不变；
- 归档存储统一到 `sessionArchive.ts`（P2 E5-S4 既有实现：workspace+instance 分区、fail-open、有单测），旧全局 key `archived_session_ids` 幂等合并迁移；
- 分页控件仅在活跃分区显示（归档区为当前页数据的本地过滤）。

**DD-7 归档语义与 Acc 关联边界**：归档是设备侧列表元数据（OpenCode 上游无 archive 写接口——沿 E5-S4 裁决），"与 Acc 任务相关连"落地为**分区语义对齐**（同一活跃/归档任务视角）；不做 session→task 数据级关联——后端 Task 仅存 `sessionCount` 聚合计数、无外键，反向不可行。数据级关联待后端提供 session.task_id 或 task.sessions[]（登记 P2）。

### 7.3 连带发现并修复的存量 bug：ScrollChromePortal 目标丢失

`#app-chrome-sub` Teleport 目标元素在 8ddbc43 引入后某次 AppLayout 重构中丢失 → **sessions/email/instances/meetings/vault 五个视图的搜索工具栏静默不渲染**（含本次新增的分区切换）。恢复目标元素（AppLayout 顶栏之下，`v-if=showTopBar`，:empty 不占位）。真机确认：搜索框 + 分段切换渲染恢复（chrome-sub 高 111px）。

### 7.4 追加轮验证（vivo X Fold5，23:43–23:5x）

| # | 验证点 | 结果 |
|---|---|---|
| V-9 | 详情页头部距顶 | ✅ 90px → **39px**（vivo-p15-07） |
| V-10 | /ai 顶栏标题文字位置 | ✅ ~78px → 51px |
| V-11 | 分区切换渲染（活跃20/归档0，默认活跃） | ✅（vivo-p15-08） |
| V-12 | 归档过滤（注入归档态 → 活跃19/归档1） | ✅ |
| V-13 | 归档区徽标 + 空态提示 | ✅（vivo-p15-09/10） |
| V-14 | 左滑恢复动作（合成手势 → 活跃20/归档0） | ✅ |
| V-15 | 五视图工具栏恢复（chrome-sub 渲染） | ✅ |

### 7.5 后续（不属本期）

- RoundTimeline 轮头信息密度（P2 折叠屏形态层一并处理）
- ⋮ 抽屉内"导出/统计"的使用率观察（若极低可进一步收纳）
- targets 可切换模式的 chip 回归验证（P2 接线时）
- 并行流三文件（AIChatView/Gateway 两页）的 safe-area 双重 padding 核查（交接属主）
- session→task 数据级关联（待后端外键，DD-7）
